/**
 * This type represents additional information for a data clump type, such as cluster information.
 */
export type DataClumpTypeAdditional = {
    /** The identifier of the cluster this data clump belongs to. */
    cluster_id?: number;
    /** The type of the cluster this data clump belongs to. */
    cluster_type?: number;
}
